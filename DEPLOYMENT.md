# 배포 가이드

## 🚀 빠른 시작

### 1. GitHub 레포지토리 생성

#### 방법 1: GitHub 웹사이트에서 생성

1. [GitHub](https://github.com)에 로그인
2. 우측 상단의 "+" 버튼 클릭 > "New repository" 선택
3. 레포지토리 이름 입력: `journal-landing-page` (또는 원하는 이름)
4. Public으로 설정 (GitHub Pages 무료 사용을 위해)
5. "Create repository" 클릭

#### 방법 2: GitHub CLI 사용 (선택사항)

```bash
gh repo create journal-landing-page --public
```

### 2. 로컬 레포지토리 초기화

```bash
cd landing-page

# Git 초기화
git init

# 파일 추가
git add .

# 커밋
git commit -m "Initial commit: Landing page setup"

# 원격 저장소 연결
git remote add origin https://github.com/netsgo0319/journal-landing-page.git

# 메인 브랜치로 이름 변경
git branch -M main

# 푸시
git push -u origin main
```

### 3. 의존성 설치

```bash
npm install
```

### 4. 로컬 개발 서버 실행

```bash
npm run dev
```

브라우저에서 `http://localhost:3001`로 접속하여 확인합니다.

### 5. 빌드 테스트

```bash
npm run build
```

빌드가 성공하면 `dist` 폴더에 정적 파일이 생성됩니다.

### 6. GitHub Pages 설정

1. GitHub 레포지토리 페이지로 이동
2. **Settings** 탭 클릭
3. 왼쪽 메뉴에서 **Pages** 클릭
4. **Source** 섹션에서:
   - **Deploy from a branch** 선택 (기본값)
   - **Branch**에서 `gh-pages` 선택 (또는 GitHub Actions 사용)
   - **Folder**에서 `/ (root)` 선택
   - **Save** 클릭

**또는 GitHub Actions 사용 (권장)**:

1. **Settings** > **Pages**로 이동
2. **Source**에서 **GitHub Actions** 선택
3. 저장 (이미 `.github/workflows/deploy.yml` 파일이 있으므로 자동으로 인식됩니다)

### 7. 자동 배포 확인

1. `main` 브랜치에 푸시하면 자동으로 배포됩니다
2. **Actions** 탭에서 배포 진행 상황을 확인할 수 있습니다
3. 배포가 완료되면 `https://netsgo0319.github.io/journal-landing-page`에서 사이트를 확인할 수 있습니다

## 🔧 추가 설정

### 커스텀 도메인 설정 (선택사항)

1. `public` 폴더에 `CNAME` 파일 생성:
   ```bash
   echo "landing.example.com" > public/CNAME
   ```

2. Git에 추가 및 커밋:
   ```bash
   git add public/CNAME
   git commit -m "Add custom domain"
   git push
   ```

3. DNS 설정:
   - A 레코드: `185.199.108.153`, `185.199.109.153`, `185.199.110.153`, `185.199.111.153`
   - 또는 CNAME 레코드: `yourusername.github.io`

4. GitHub 레포지토리 Settings > Pages에서 도메인 추가 및 확인

### 환경 변수 설정 (필요시)

현재는 환경 변수가 필요하지 않지만, 향후 추가할 수 있습니다.

`.env` 파일 생성:
```bash
VITE_API_URL=https://api.example.com
```

## 📝 주의사항

1. **Public 레포지토리**: GitHub Pages는 Public 레포지토리에서만 무료로 사용 가능합니다.
2. **배포 시간**: 배포에는 몇 분이 소요될 수 있습니다.
3. **URL 활성화**: 첫 배포 후 URL이 활성화되는 데 시간이 걸릴 수 있습니다 (최대 10분).
4. **빌드 실패**: 빌드가 실패하면 Actions 탭에서 에러를 확인하세요.

## 🐛 문제 해결

### 배포가 실패하는 경우

1. **Actions 탭에서 에러 로그 확인**
   - 레포지토리 > Actions 탭
   - 실패한 워크플로우 클릭
   - 에러 메시지 확인

2. **로컬 빌드 테스트**
   ```bash
   npm run build
   ```
   - 로컬에서 빌드가 성공하는지 확인
   - 에러가 있으면 수정 후 다시 푸시

3. **GitHub Actions 권한 설정 확인**
   - Settings > Actions > General
   - "Workflow permissions"에서 "Read and write permissions" 선택
   - "Allow GitHub Actions to create and approve pull requests" 체크

### 사이트가 표시되지 않는 경우

1. **배포 완료 확인**
   - Actions 탭에서 배포가 완료되었는지 확인
   - 초록색 체크 표시가 있는지 확인

2. **URL 확인**
   - URL이 정확한지 확인: `https://netsgo0319.github.io/journal-landing-page`
   - 레포지토리 이름이 URL과 일치하는지 확인

3. **브라우저 캐시**
   - 브라우저 캐시를 지우고 다시 시도
   - 시크릿 모드에서 테스트

4. **빌드 파일 확인**
   - `dist` 폴더에 `index.html`이 있는지 확인
   - 빌드가 성공적으로 완료되었는지 확인

### 빌드 에러가 발생하는 경우

1. **의존성 문제**
   ```bash
   rm -rf node_modules package-lock.json
   npm install
   ```

2. **TypeScript 에러**
   ```bash
   npm run build
   ```
   - 에러 메시지를 확인하고 수정

3. **Tailwind CSS 에러**
   - `tailwind.config.js` 파일이 올바른지 확인
   - `postcss.config.js` 파일이 있는지 확인

## 📚 참고 자료

- [GitHub Pages 문서](https://docs.github.com/pages)
- [GitHub Actions 문서](https://docs.github.com/actions)
- [Vite 문서](https://vitejs.dev/)
- [React 문서](https://react.dev/)
- [Tailwind CSS 문서](https://tailwindcss.com/)

## 🎉 배포 완료 후

배포가 완료되면:

1. 사이트 URL을 공유할 수 있습니다
2. SEO 최적화가 적용되어 검색 엔진에 노출됩니다
3. GitHub Actions를 통해 자동으로 배포됩니다
4. 커스텀 도메인을 설정할 수 있습니다

---

**작성일**: 2025-01-27
**최종 수정일**: 2025-01-27

